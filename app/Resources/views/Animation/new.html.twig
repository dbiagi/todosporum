{% extends 'layout.html.twig' %}

{% block content %}
    <div class="container">
        <h1>Animation creation</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
        {{ form_end(form) }}

        <ul>
            <li>
                <a href="{{ path('animation_index') }}">Back to the list</a>
            </li>
        </ul>
    </div>
{% endblock %}
